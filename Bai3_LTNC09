void solve(double meal_cost, double tip_percent, double tax_percent) {
    double tip = (meal_cost / 100.00) * tip_percent;
    double tax = (tax_percent / 100.00) * meal_cost;
    double total = meal_cost + tip + tax;
    cout << round(total);
}
